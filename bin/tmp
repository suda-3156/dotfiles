#!/usr/bin/env bash
#
# Create a directory on Desktop and move files into it.
#

if [[ -z "$1" ]]; then
  mkdir -p ~/Desktop/tmp
  touch ~/Desktop/tmp/"$(date +'%Y%m%d%H%M%S')".txt && ${EDITOR:-vi} "$_"
else
  mkdir -p ~/Desktop/tmp
  mv "$1" ~/Desktop/tmp/
fi
